<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:id="@+id/preview_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#99000000"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="14dp"
        android:text="Point the camera at the bike's MotoPlay QR\n\nIf it won't scan or connect — on the dash, switch to the iOS / CarPlay QR and try that one"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:lineSpacingExtra="2dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="88dp"
        android:background="#88000000"
        android:orientation="horizontal"
        android:padding="6dp">

        <Button
            android:id="@+id/btn_zoom_1x"
            android:layout_width="56dp"
            android:layout_height="44dp"
            android:layout_marginEnd="4dp"
            android:minWidth="0dp"
            android:padding="0dp"
            android:text="1x"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btn_zoom_2x"
            android:layout_width="56dp"
            android:layout_height="44dp"
            android:layout_marginEnd="4dp"
            android:minWidth="0dp"
            android:padding="0dp"
            android:text="2x"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btn_zoom_3x"
            android:layout_width="56dp"
            android:layout_height="44dp"
            android:minWidth="0dp"
            android:padding="0dp"
            android:text="3x"
            android:textColor="#FFFFFF" />
    </LinearLayout>
</FrameLayout>
